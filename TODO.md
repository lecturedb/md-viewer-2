# TODO: React 실시간 마크다운 뷰어 구현

## Phase 1. 프로젝트 준비 및 의존성 설정

### Doing

- [x] `PRD.md`의 필수 범위와 제외 범위를 다시 확인한다.
- [x] 마크다운 렌더링을 위해 `react-markdown`을 설치하고 `package-lock.json`을 갱신한다.
- [x] 현재 Vite + React + TypeScript 구조를 유지하며 필요한 파일 위치를 정한다.
- [x] 구현 전 `src/App.tsx`, `src/App.css`, `src/index.css`의 역할을 정리한다.

### Don't

- [x] 저장, 불러오기, 다운로드, 다크 모드, 계정 기능을 추가하지 않는다.
- [x] 마크다운 파서를 직접 구현하지 않는다.
- [x] 테스트 프레임워크나 상태 관리 라이브러리를 새로 도입하지 않는다.

### Success Criteria

- [x] `npm install` 후 의존성이 정상 설치된다.
- [x] `package.json`에 필요한 최소 의존성만 추가되어 있다.
- [x] `npm run lint`와 `npm run build` 실행 준비가 되어 있다.
- [x] 구현 범위가 `PRD.md`의 초기 버전 요구사항을 벗어나지 않는다.

## Phase 2. 기본 레이아웃 및 입력 상태 구현

### Doing

- [x] `App.tsx`에 마크다운 입력 상태를 `useState`로 구성한다.
- [x] 제목, 문단, 목록, 코드 블록이 포함된 기본 예시 마크다운을 초기값으로 추가한다.
- [x] 데스크톱 기준 좌측 입력 영역과 우측 미리보기 영역을 5:5 비율로 배치한다.
- [x] 입력 영역에 명확한 라벨과 충분한 높이, 독립 스크롤을 적용한다.

### Don't

- [x] 복잡한 에디터 라이브러리나 커스텀 문법 도구를 도입하지 않는다.
- [x] 패널 리사이즈, 스크롤 동기화, 단축키 기능을 구현하지 않는다.
- [x] 모바일 탭 전환을 이 단계에서 무리하게 완성하지 않는다.

### Success Criteria

- [x] 데스크톱 화면에서 입력 패널과 미리보기 패널이 나란히 표시된다.
- [x] 입력 영역에 기본 예시 마크다운이 표시된다.
- [x] 사용자가 입력한 텍스트가 React 상태에 즉시 반영된다.
- [x] 긴 입력 내용이 레이아웃을 깨뜨리지 않고 스크롤된다.

## Phase 3. 마크다운 렌더링 및 문서 스타일링

### Doing

- [x] `react-markdown`으로 입력 상태를 미리보기 영역에 렌더링한다.
- [x] H1~H6, 문단, 블릿 목록, 숫자 목록, 인라인 코드, 코드 블록 스타일을 정의한다.
- [x] 코드 블록에 고정폭 폰트, 배경, 패딩, 가로 스크롤을 적용한다.
- [x] 입력이 비어 있을 때 표시할 간결한 빈 상태를 구현한다.

### Don't

- [x] 코드 문법 강조 라이브러리를 필수 기능처럼 추가하지 않는다.
- [x] GitHub Flavored Markdown 전체 지원을 목표로 범위를 넓히지 않는다.
- [x] 렌더링 결과를 서버나 별도 API에 의존하게 만들지 않는다.

### Success Criteria

- [x] 입력 변경 시 미리보기가 새로고침 없이 즉시 갱신된다.
- [x] 제목 H1~H6이 계층별 크기와 여백으로 구분된다.
- [x] 문단과 목록이 읽기 좋은 간격으로 표시된다.
- [x] 코드 블록과 인라인 코드가 일반 텍스트와 명확히 구분된다.

## Phase 4. 반응형 UI 및 접근성 마무리

### Doing

- [x] 768px 안팎의 브레이크포인트를 기준으로 모바일 레이아웃을 적용한다.
- [x] 모바일에서 입력과 미리보기를 상하 배치하거나 탭 전환 방식으로 구성한다.
- [x] 포커스 상태, 텍스트 대비, 라벨 연결 등 기본 접근성을 점검한다.
- [x] 긴 단어와 긴 코드 줄이 작은 화면에서 UI를 깨뜨리지 않도록 처리한다.

### Don't

- [x] 모바일 전용으로 완전히 다른 앱 구조를 만들지 않는다.
- [x] 장식 요소나 과한 카드 UI로 미니멀 스타일을 흐리지 않는다.
- [x] PRD의 제외 범위에 있는 다중 문서 관리나 테마 설정을 추가하지 않는다.

### Success Criteria

- [x] 모바일 폭에서 입력 영역과 미리보기 영역을 모두 사용할 수 있다.
- [x] 코드 블록이 화면 밖으로 레이아웃을 밀어내지 않는다.
- [x] 키보드만으로 주요 입력과 컨트롤에 접근할 수 있다.
- [x] 전체 UI가 밝고 깔끔한 미니멀 스타일을 유지한다.

## Phase 5. 검증 및 정리

### Doing

- [x] `npm run lint`로 ESLint 오류를 확인한다.
- [x] `npm run build`로 TypeScript 및 Vite 빌드를 검증한다.
- [x] `npm run dev`에서 데스크톱 화면의 5:5 레이아웃과 실시간 렌더링을 수동 확인한다.
- [x] 모바일 viewport에서 반응형 레이아웃과 코드 블록 표시를 수동 확인한다.

### Don't

- [x] `dist/` 변경을 결과물로 관리하거나 커밋 대상으로 삼지 않는다.
- [x] 검증 중 발견한 범위 밖 개선 아이디어를 즉시 구현하지 않는다.
- [x] PRD와 무관한 리팩터링으로 변경 범위를 넓히지 않는다.

### Success Criteria

- [x] `npm run lint`가 통과한다.
- [x] `npm run build`가 통과한다.
- [x] 데스크톱에서 입력과 미리보기가 5:5 비율로 안정적으로 표시된다.
- [x] 모바일에서 입력, 미리보기, 코드 블록이 가독성 있게 표시된다.
- [x] 실시간 렌더링이 입력 즉시 동작하는 것이 확인된다.
